# Domain docs

This repo uses a **multi-context** layout.

- `CONTEXT-MAP.md` at the repo root maps contexts to their `CONTEXT.md` files
- Each context has its own `CONTEXT.md` + `docs/adr/` directory
- Skills read the relevant `CONTEXT.md` for domain language and `docs/adr/` for architectural decisions
